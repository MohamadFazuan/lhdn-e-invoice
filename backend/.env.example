# Cloudflare Worker secrets — copy to .dev.vars for local development
# Generate JWT secrets and ENCRYPTION_KEY with: openssl rand -hex 32

JWT_ACCESS_SECRET=einvoislhdn2026
JWT_REFRESH_SECRET=einvoislhdn2026
ENCRYPTION_KEY=einvoislhdn2026

# R2 API Token — create at: Cloudflare Dashboard → R2 → Manage R2 API tokens
# Required for aws4fetch presigned URL generation
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_ACCOUNT_ID=

# Non-secret vars (also in wrangler.toml [vars])
NODE_ENV=development
LHDN_ENV=sandbox
BCRYPT_SALT_ROUNDS=10
MAX_FILE_SIZE_MB=10
R2_FILE_BUCKET_NAME=lhdn-einvoice-files

# MySQL (Phase 2 VPS migration only)
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=
MYSQL_DATABASE=lhdn_einvoice

# Drizzle Kit — D1 HTTP (for drizzle-kit studio/generate with live D1)
CLOUDFLARE_ACCOUNT_ID=
CLOUDFLARE_D1_DATABASE_ID=
CLOUDFLARE_D1_TOKEN=
